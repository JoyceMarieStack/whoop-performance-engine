# Whoop OAuth2 Application Credentials
# Register your app at https://developer.whoop.com to obtain these values
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REDIRECT_URI=http://localhost:3000/callback

# Server port
PORT=3000

# Dynamic tokens — managed by the app after OAuth authorization
# Leave these empty; they are populated automatically after first login
WHOOP_ACCESS_TOKEN=
WHOOP_REFRESH_TOKEN=
WHOOP_TOKEN_EXPIRES_AT=


